# Environment (development or production)
NODE_ENV="development"

# Database - Use different databases for dev and prod
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# JWT - Use strong secrets in both environments
JWT_SECRET="generate-a-strong-secret-for-each-environment"

# Email - Use different API keys for dev and prod
SENDGRID_API_KEY="your-sendgrid-api-key"
FROM_EMAIL="noreply@editai.app"

# App URL - Automatically set based on environment
# Development: http://localhost:3000
# Production: https://www.editai.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Note: Never commit real credentials. Use this file as a template.
# Copy this file to .env.local for development
# Set environment variables in your deployment platform for production 
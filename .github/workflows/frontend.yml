name: Frontend CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'apps/frontend/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'apps/frontend/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install Dependencies
      run: |
        cd apps/frontend
        npm install
        
    - name: Build
      run: |
        cd apps/frontend
        npm run build
        
    - name: Run Tests
      run: |
        cd apps/frontend
        npm test
      
    # Deploy step will be added once we have hosting setup
    # - name: Deploy
    #   if: github.ref == 'refs/heads/main'
    #   run: |
    #     # Deploy to production 